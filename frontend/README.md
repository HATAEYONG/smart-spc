# SPC 품질관리 시스템 - Frontend

AI LLM 기반 통계적 공정 관리(SPC) 시스템의 React + TypeScript 프론트엔드

## 🚀 빠른 시작

### 1. 의존성 설치

```bash
npm install
```

### 2. 개발 서버 실행

```bash
npm run dev
```

브라우저에서 http://localhost:3000 접속

### 3. 프로덕션 빌드

```bash
npm run build
npm run preview
```

## 📦 기술 스택

| 기술 | 버전 | 용도 |
|-----|------|------|
| React | 18.2+ | UI 프레임워크 |
| TypeScript | 5.2+ | 타입 안정성 |
| Vite | 5.0+ | 빌드 도구 |
| React Router | 6.20+ | 라우팅 |
| Recharts | 2.10+ | 데이터 시각화 |
| Axios | 1.6+ | HTTP 클라이언트 |
| Tailwind CSS | 3.3+ | 스타일링 |

## 🏗️ 프로젝트 구조

```
frontend/
├── src/
│   ├── pages/                    # 페이지 컴포넌트
│   │   ├── SPCDashboardPage.tsx       # SPC 대시보드
│   │   ├── ProcessCapabilityPage.tsx  # 공정능력 분석
│   │   ├── DataEntryPage.tsx          # 데이터 입력 (✅ 완료)
│   │   └── RunRuleAnalysisPage.tsx    # Run Rule 분석 (✅ 완료)
│   │
│   ├── components/               # 재사용 컴포넌트
│   │   ├── XBarRChart.tsx             # X-bar & R Chart
│   │   ├── PChart.tsx                 # p-Chart (불량률) ✅
│   │   ├── CChart.tsx                 # c-Chart (결점수) ✅
│   │   └── RunRuleViolationsChart.tsx # Run Rule 시각화 ✅
│   │
│   ├── services/                 # API 서비스
│   │   └── api.ts                     # Backend API 클라이언트
│   │
│   ├── types/                    # TypeScript 타입
│   │   └── spc.ts                     # SPC 타입 정의
│   │
│   ├── App.tsx                   # 메인 앱 (4개 라우트)
│   ├── main.tsx                  # 엔트리 포인트
│   └── index.css                 # 글로벌 스타일
│
├── .env.example                 # 환경 변수 예시 ✅
├── package.json
├── vite.config.ts
├── tsconfig.json
└── tailwind.config.js
```

## 📊 주요 기능

### 1. SPC 대시보드 (`/`) ✅

**기능:**
- 제품별 품질 요약 통계
- 실시간 X-bar & R Chart 시각화
- 품질 경고 모니터링
- 최근 경고 목록

**엔드포인트:**
- GET `/api/spc/products/` - 제품 목록
- GET `/api/spc/products/{id}/summary/` - 제품 요약
- GET `/api/spc/measurements/chart_data/` - 관리도 데이터
- GET `/api/spc/alerts/dashboard/` - 경고 대시보드

### 2. 공정능력 분석 (`/capability`) ✅

**기능:**
- Cp, Cpk, Pp, Ppk 계산
- 공정능력 지수 평가
- 통계 정보 (평균, 표준편차, 정규성 검정)
- 지수 비교 차트

**엔드포인트:**
- POST `/api/spc/process-capability/analyze/` - 분석 실행
- GET `/api/spc/process-capability/` - 분석 결과 조회

### 3. 데이터 입력 (`/data-entry`) ✅

**기능:**
- 수동 측정 데이터 입력 폼
- CSV 파일 일괄 업로드
- 최근 입력 데이터 목록
- 규격 이탈 자동 판정

**엔드포인트:**
- POST `/api/spc/measurements/` - 측정 데이터 생성
- POST `/api/spc/measurements/bulk_create/` - CSV 일괄 업로드

### 4. Run Rule 분석 (`/run-rules`) ✅

**기능:**
- Western Electric Rules 위반 시각화
- 8가지 Run Rule 타입별 색상 표시
- 위반 요약 통계
- 상세 위반 목록 테이블
- Run Rule 설명 가이드

**엔드포인트:**
- GET `/api/spc/run-rule-violations/` - 위반 목록
- POST `/api/spc/run-rule-violations/check/` - Run Rule 검사

### 5. 관리도 컴포넌트

**X-bar & R Chart** ✅
- 평균 관리도 (X-bar Chart)
- 범위 관리도 (R Chart)
- 관리 한계선 (UCL, CL, LCL) 시각화
- 관리 이탈 점 강조 표시

**p-Chart (불량률)** ✅
- 불량률(%) 시각화
- 이항분포 기반 관리 한계선

**c-Chart (결점수)** ✅
- 결점 수 시각화
- 포아송 분포 기반 관리 한계선

**Run Rule 위반 차트** ✅
- 8가지 Run Rule 타입별 색상 구분
- 1σ, 2σ 보조선 표시
- 위반 점 강조 및 상세 목록

## 🔧 API 통신

### API 클라이언트 (`services/api.ts`)

```typescript
import { spcApi } from '@/services/api';

// 제품 목록 조회
const products = await spcApi.products.list({ is_active: true });

// 관리도 데이터 조회
const chartData = await spcApi.measurements.chartData({
  product_id: 1,
  chart_type: 'XBAR_R',
});

// 공정능력 분석 실행
const capability = await spcApi.processCapability.analyze({
  product_id: 1,
  start_date: '2024-01-01',
  end_date: '2024-01-31',
  chart_type: 'XBAR_R',
});
```

### 사용 가능한 API 모듈

- `spcApi.products` - 제품 관리
- `spcApi.inspectionPlans` - 검사 계획
- `spcApi.measurements` - 측정 데이터
- `spcApi.controlCharts` - 관리도
- `spcApi.processCapability` - 공정능력 분석
- `spcApi.runRuleViolations` - Run Rule 위반
- `spcApi.qualityAlerts` - 품질 경고
- `spcApi.reports` - 품질 보고서

## 🎨 컴포넌트 사용 예시

### X-bar & R Chart

```tsx
import { XBarRChart } from '@/components/XBarRChart';

const MyComponent = () => {
  const chartData = {
    data: [
      { subgroup_number: 1, xbar: 10.2, r: 0.3, measured_at: '2024-01-01T10:00:00Z' },
      { subgroup_number: 2, xbar: 10.1, r: 0.2, measured_at: '2024-01-01T11:00:00Z' },
      // ...
    ],
    limits: {
      xbar: { ucl: 10.5, cl: 10.0, lcl: 9.5 },
      r: { ucl: 0.8, cl: 0.4, lcl: 0.0 },
    },
  };

  return <XBarRChart data={chartData.data} limits={chartData.limits} />;
};
```

## 📱 화면 구성

### 대시보드 페이지 (`/`)
```
┌─────────────────────────────────────────────┐
│  제품 선택: [Dropdown]                      │
├─────────────────────────────────────────────┤
│  ┌────────────────┐  ┌──────────────────┐  │
│  │  총 측정: 100  │  │  규격이탈: 2%   │  │
│  └────────────────┘  └──────────────────┘  │
├─────────────────────────────────────────────┤
│  X-bar Chart                                │
│  [관리도 그래프]                            │
│                                             │
│  R Chart                                    │
│  [관리도 그래프]                            │
├─────────────────────────────────────────────┤
│  최근 경고:                                 │
│  ⚠️ 관리 한계 벗어남 (긴급)               │
│  ⚠️ 9개 연속 중심선 한쪽 (높음)           │
└─────────────────────────────────────────────┘
```

### 공정능력 분석 페이지 (`/capability`)
```
┌─────────────────────────────────────────────┐
│  제품: [Dropdown]  시작: [Date]  종료: [Date]│
│  [분석 실행 버튼]                           │
├─────────────────────────────────────────────┤
│  공정능력 지수:                             │
│  Cp:  1.45   Cpk: 1.33                     │
│  Pp:  1.40   Ppk: 1.30                     │
│  평가: 보통 (허용 가능)                     │
├─────────────────────────────────────────────┤
│  [지수 비교 차트]                           │
│  [Bar Chart: Cp, Cpk, Pp, Ppk]             │
└─────────────────────────────────────────────┘
```

## 🔐 환경 변수

`.env` 파일 생성:

```env
VITE_API_BASE_URL=http://localhost:8000/api/spc
```

## 🐛 디버깅

### Backend 연결 확인
```bash
# Backend가 실행 중인지 확인
curl http://localhost:8000/api/spc/products/
```

### CORS 에러 해결
Backend `settings.py`에 CORS 설정 추가:
```python
CORS_ALLOWED_ORIGINS = [
    "http://localhost:3000",
]
```

## 📦 배포

### 1. 빌드
```bash
npm run build
```

### 2. Nginx 설정 예시
```nginx
server {
    listen 80;
    server_name spc.example.com;

    root /var/www/spc-frontend/dist;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    location /api {
        proxy_pass http://localhost:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
```

## 🎉 완료된 기능

- [x] SPC 대시보드 페이지
- [x] 공정능력 분석 페이지
- [x] 데이터 입력 페이지 (수동 + CSV 업로드)
- [x] Run Rule 분석 페이지
- [x] X-bar & R Chart 컴포넌트
- [x] p-Chart (불량률) 컴포넌트
- [x] c-Chart (결점수) 컴포넌트
- [x] Run Rule 위반 시각화 컴포넌트
- [x] API 클라이언트 완전 구현
- [x] TypeScript 타입 정의 완료
- [x] 반응형 UI (Tailwind CSS)

## 🧪 추가 개발 예정 (Phase 2)

- [ ] AI 챗봇 통합
- [ ] 품질 보고서 생성 (PDF/Excel)
- [ ] 실시간 WebSocket 알림
- [ ] CUSUM Chart, EWMA Chart
- [ ] 다변량 관리도 (Hotelling's T²)
- [ ] 테스트 코드 작성
- [ ] 다크 모드 지원
- [ ] 다국어 지원

## 📄 라이선스

MIT License

## 🤝 기여

이슈 및 Pull Request 환영합니다!

---

**문서 작성일**: 2026-01-11
**버전**: 1.0.0
